# Godot editor cache
.godot/

# Mono/C# build output
.mono/
bin/
obj/

# Export presets (contains platform-specific paths)
export_presets.cfg

# macOS
.DS_Store

# Import files are generated by Godot; *.import are usually excluded
# unless you want to version them (needed for some asset pipelines)
*.import
